我已经将Qwen相关文件全部下载到如下文件夹 dify-langgraph-qwen-demo/data/models/Qwen2.5-14B-Instruct-AWQ，更改配置参数，直接从该文件夹加载文件

I have downloaded all Qwen-related files to the following folder: dify-langgraph-qwen-demo/data/models/Qwen2.5-14B-Instruct-AWQ. Please change the configuration parameters to load the files directly from this folder.


Launch Sequence (Summary)
To deploy and test, navigate to dify-langgraph-qwen-demo and execute in this order:

Diagnose host capabilities: bash qwen_server/check_gpu.sh
Start the model server: bash qwen_server/start_qwen_vllm_docker.sh (or bash qwen_server/start_qwen_vllm.sh)
Test connectivity: python qwen_server/test_qwen_api.py
Start the agent server: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
Test integrated agent flow: python scripts/test_agent.py
'


python scripts/infer_rag_single.py --config configs/rag.yaml --final-snapshot examples/final_snapshot.png --prev-depth-map examples/prev_depth_map.png --output-dir outputs/rag_single



Now create the first runnable version of this project.

Goal of this phase:
Build a clean, extensible project skeleton for a provider-agnostic AI agent base. MiniMax must be the default provider, but the system must be designed so that I can later switch to other API providers or locally deployed models.

Do not try to implement every MiniMax API detail in this phase. Focus on a clean architecture, runnable backend, runnable frontend, provider abstraction, LangGraph integration, and one working minimal task.

Tech stack:

* Backend: Python 3.11+, FastAPI, Pydantic, LangGraph
* Frontend: React + Vite + TypeScript
* Configuration: `.env`
* Default provider: MiniMax
* Optional provider: OpenAI-compatible API
* The backend must start even if API keys are missing.


Backend requirements:

1. Create a `BaseModelProvider` abstract class with at least:

   * `provider_name`
   * `is_available()`
   * `chat(request)`
   * placeholder methods for image, image-to-image, video, speech, music, and file tasks.

2. Create `MiniMaxProvider`.

   * It should read configuration from environment variables.
   * It should be the default provider.
   * If `MINIMAX_API_KEY` is missing, it should be disabled gracefully.
   * Do not crash the backend when the key is missing.
   * For this phase, implement at least the `chat` method.
   * If the exact MiniMax chat API details are uncertain, isolate the code clearly and add TODO comments instead of inventing fake parameters.

3. Create `OpenAICompatibleProvider`.

   * It should read:

     * `OPENAI_COMPATIBLE_BASE_URL`
     * `OPENAI_COMPATIBLE_API_KEY`
     * `OPENAI_COMPATIBLE_MODEL`
   * It should be optional.
   * It should not be the default provider.

4. Create a provider registry.

   * It should list available providers.
   * It should return the selected provider.
   * It should fall back safely if a provider is unavailable.

5. Create FastAPI endpoints:

   * `GET /api/health`
   * `GET /api/providers`
   * `POST /api/chat`
   * `POST /api/agent/run`

6. Create a simple LangGraph workflow.

   * It should accept a user message.
   * It should select the provider.
   * It should call the provider’s `chat` method.
   * It should return a structured trace.

7. Use a consistent response format:

   ```json
   {
     "success": true,
     "provider": "minimax",
     "task_type": "chat",
     "data": {},
     "trace": [],
     "error": null
   }
   ```

Frontend requirements:

1. Create a simple React/Vite frontend.
2. Add a Settings page that displays available providers.
3. Add a Chat page.
4. The user should be able to:

   * select a provider
   * enter a message
   * send it to `/api/chat` or `/api/agent/run`
   * see the response
   * see the trace
   * see errors clearly
5. The frontend must not expose or request API keys.

Documentation requirements:

1. Add `README.md` with setup instructions:

   * install backend dependencies
   * run backend
   * install frontend dependencies
   * run frontend
   * configure `.env`

2. Add `docs/architecture.md`.

   * Explain provider abstraction.
   * Explain why MiniMax is only one provider adapter.
   * Explain how LangGraph calls providers.

3. Add `docs/provider_adapter_guide.md`.

   * Explain how to add a new provider.

4. Add `docs/dify_http_node_examples.md`.

   * Provide example requests for Dify HTTP Request nodes calling:

     * `/api/chat`
     * `/api/agent/run`

Testing requirements:

1. Add minimal backend tests.
2. Test that the app starts when API keys are missing.
3. Test provider registry behavior.
4. Test `/api/health`.

Important constraints:

* Do not implement unrestricted shell execution.
* Do not implement local file modification yet.
* Do not implement all MiniMax multimodal APIs yet.
* Keep this phase small, clean, and runnable.
* Prefer architecture quality over feature completeness.
* After implementation, give me:

  1. summary of created files
  2. how to run backend
  3. how to run frontend
  4. what is implemented
  5. what remains TODO





