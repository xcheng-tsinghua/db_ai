version: '3.8'

services:
  agent-backend:
    build: .
    image: dify-langgraph-qwen-backend:latest
    container_name: dify-langgraph-qwen-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
      - HOST=0.0.0.0
      - PORT=8000
      - DEBUG=true

#  # Optional: Uncomment below to run the Qwen Model Server inside Docker Compose
#  # Ensure NVIDIA Container Toolkit is installed and active on the host daemon.
#  qwen-server:
#    image: vllm/vllm-openai:latest
#    container_name: qwen-vllm-server
#    runtime: nvidia
#    deploy:
#      resources:
#        reservations:
#          devices:
#            - driver: nvidia
#              count: all
#              capabilities: [gpu]
#    ports:
#      - "${QWEN_PORT:-8001}:${QWEN_PORT:-8001}"
#    env_file:
#      - .env
#    volumes:
#      # Option A: Online loading (download from Hugging Face)
#      - ~/.cache/huggingface:/root/.cache/huggingface
#      # Option B: Offline loading (load from local model directory)
#      # - ./data/models/Qwen2.5-14B-Instruct-AWQ:/model
#    ipc: host
#    environment:
#      - VLLM_API_KEY=${QWEN_API_KEY:-EMPTY}
#    command: >
#      --model ${QWEN_MODEL_PATH:-Qwen/Qwen2.5-14B-Instruct-AWQ}
#      # Note: If using Option B (local folder), change '--model ...' above to '--model /model'
#      --served-model-name ${QWEN_SERVED_MODEL_NAME:-qwen14b}
#      --host ${QWEN_HOST:-0.0.0.0}
#      --port ${QWEN_PORT:-8001}
#      --max-model-len ${QWEN_MAX_MODEL_LEN:-8192}
#      --gpu-memory-utilization ${QWEN_GPU_MEMORY_UTILIZATION:-0.90}
#      --max-num-seqs ${QWEN_MAX_NUM_SEQS:-2}
#      --dtype ${QWEN_DTYPE:-auto}

